# Public site root URL (no trailing slash).
# Used by: Vite client (canonical / Open Graph), and scripts/generate-sitemap.mjs (sitemap + robots).
# Copy to `.env` and set your real domain, e.g. https://your-site.netlify.app
VITE_SITE_URL=
